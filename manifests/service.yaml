apiVersion: v1
kind: Service
metadata:
  name: capstone-phoenix-service
spec:
  type: LoadBalancer
  selector:
    app: capstone-phoenix
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
